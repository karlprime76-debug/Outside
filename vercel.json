{
  "buildCommand": "prisma generate && (prisma migrate deploy || echo '⚠️ Migration deferred — run manually via Vercel CLI or dashboard') && next build",
  "crons": [
    {
      "path": "/api/cron/plan-reminders",
      "schedule": "*/5 * * * *"
    }
  ]
}
