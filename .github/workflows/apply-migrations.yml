name: Apply Prisma Migrations

on:
  workflow_dispatch:
    inputs:
      run_seed:
        description: 'Run demo seed after migration'
        required: true
        default: 'false'
        type: choice
        options: ['true', 'false']

<<<<<<< HEAD
=======
env:
  MIGRATION_NAME: "20240607_add_referral_code"

>>>>>>> c3e0dde (fix: correct migration name and make referral_code SQL idempotent)
jobs:
  migrate:
    runs-on: ubuntu-latest
    env:
      DATABASE_URL: ${{ secrets.DATABASE_URL }}
      DIRECT_URL: ${{ secrets.CI_DIRECT_URL }}
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci
        timeout-minutes: 10

      - name: Generate Prisma client
        run: npx prisma generate
        timeout-minutes: 5

      - name: Step 1 — Apply pending migrations normally
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
<<<<<<< HEAD
          echo "=== Apply any pending migrations ==="
          npx prisma migrate deploy
          echo "✓ migrate deploy done."
=======
          set +e
          MIGRATION_FILE="prisma/migrations/$MIGRATION_NAME/migration.sql"
          DB_URL="${DIRECT_URL:-$DATABASE_URL}"
>>>>>>> 8358700 (fix: restore comprehensive migration SQL and use --url for db execute)

      - name: Step 2 — Sync ALL remaining schema differences (safe, additive only)
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          echo "=== Sync all remaining tables, columns, enums ==="
          echo "Adds whatever is missing, never drops anything."
          echo "y" | npx prisma db push --skip-generate 2>&1 || echo "⚠ db push had issues (may need manual review). Continuing..."
          echo "✓ db push sync done."

      - name: Step 3 — Report final migration status
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          echo "=== Final migration status ==="
          npx prisma migrate status

<<<<<<< HEAD
=======
          echo ""
          echo "=== Step 2: Attempt normal migration deploy ==="
          npx prisma migrate deploy
          EXIT_CODE=$?

          if [ $EXIT_CODE -eq 0 ]; then
            echo "✓ Migration deployed successfully."
            exit 0
          fi

          echo ""
          echo "=== Step 3: Direct deploy failed — applying migration SQL manually ==="
          echo "Reason: likely P3005 (database has tables but no _prisma_migrations history)."
          echo "SQL is fully idempotent (CREATE TABLE IF NOT EXISTS, ADD COLUMN IF NOT EXISTS, IF NOT EXISTS)."

          npx prisma db execute --file="$MIGRATION_FILE" --url "$DB_URL"
          DB_EXEC_CODE=$?

          if [ $DB_EXEC_CODE -ne 0 ]; then
            echo "✗ prisma db execute failed (exit code $DB_EXEC_CODE)."
            exit $DB_EXEC_CODE
          fi

          echo ""
          echo "=== Step 4: Mark migration as applied in history ==="
          npx prisma migrate resolve --applied "$MIGRATION_NAME"

          echo ""
          echo "=== Step 5: Re-run migrate deploy to confirm sync ==="
          npx prisma migrate deploy

>>>>>>> c3e0dde (fix: correct migration name and make referral_code SQL idempotent)
      - name: Seed demo data (conditional)
        if: ${{ github.event.inputs.run_seed == 'true' }}
        env:
          DEMO_MODE: '1'
        run: npm run db:seed:demo
        timeout-minutes: 15
